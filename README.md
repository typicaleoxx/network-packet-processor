# network-packet-processor
This project implements a simulation of a network pracket processing system using queue data structure built on a templated circular linked list. This system models how incoming data packets are received, stored, and processed in the order they arrive, following a First-In-First-Out (FIFO) approach.
